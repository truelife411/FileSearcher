# 2026-08-01

## 霁青 UI 全面重构（延续 07-31 设计稿确认）
- 全新配色：霁青 accent #3FC1B0（深 #2FB3A3 变体），深/浅双主题，替代旧靛蓝
- FileTable 重绘：去网格线、行分隔线、类型徽章（BADGE_STYLES 类别色）、选中指示条、等宽数字列
- RoundedButton 新增 ghost 幽灵 kind（重建索引文字按钮）
- RoundedSearchBox 重写：聚焦光晕、Ctrl+F 快捷键提示胶囊、精致清空钮
- 标题栏：渐变 logo 方块 + 副标题
- 搜索区：StatusPill 胶囊组件（状态点+文案一体），索引按钮改 ghost 样式
- 结果区：卡片化容器、内嵌分页栏
- 弹窗体系全部自绘：TkContextMenu（Toplevel 圆角右键菜单）、ConfirmDialog（确认/警告/危险三态）、InputDialog（重命名/排除项）、设置窗口（左侧导航圆角选中 + 卡片内容 + 自绘 Toggle）
- 清理：删除 messagebox 全部引用、BADGE_TEXT 死常量、_manage_exclude 死代码、旧 _on_right_click 覆盖
- 语法检查 + AST 检查 + 6 秒启动冒烟均通过，已 git commit（feat: brand-new JiQing UI）

## 运行维护
- 发现两个重复 FileSearcher 实例（taskkill 报"实例不存在"异常，改用 `wmic process where ProcessId=X call terminate` 成功清理）
- 用 Python subprocess detached 方式（DETACHED|CREATE_NO_WINDOW）启动最新代码，单实例运行正常（debug.log 新增 diag 行确认）
- 教训：Git Bash 的 nohup & 后台进程可能被 shell 生命周期清理，启动 GUI 常驻程序优先用 Python Popen + DETACHED
