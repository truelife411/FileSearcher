# 2026-06-12 工作日志

## 修改关闭按钮行为
- 将 `_on_close` 从退出程序改为最小化到系统托盘 (`self.root.withdraw()`)
- 现在点 × 和点最小化效果一致，都隐藏到托盘
- 彻底退出需通过托盘右键菜单「退出」

## 托盘恢复窗口增强
- 搜索框引用改为 `self.search_entry` 以便后续操作
- `_do_restore()` 恢复窗口时：最大化窗口 + 搜索框全选文字（有文字时）或聚焦到搜索框（无文字时）

## Commit & 打包
- Git commit `42e7c9e`: 关闭按钮改为最小化到托盘；托盘恢复时最大化+搜索框全选
- 重新 PyInstaller 打包 → `C:\Users\hjf\Desktop\File Searcher.exe`
- 已 push 到 GitHub
