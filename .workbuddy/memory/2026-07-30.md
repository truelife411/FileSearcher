# 2026-07-30

- 只读分析 `file-searcher/file_searcher.py` 的搜索性能：确认主要瓶颈为 `LIKE '%关键词%'` 导致普通索引失效、搜索与最多 5000 行 Treeview 渲染均在 Tk 主线程、默认按未建索引的 `size` 排序，以及“类型”排序会读取全量记录后在 Python 中排序。
- 建议优先实施：页面大小降至约 300、查询移入后台线程并丢弃过期请求、增加 `size/modified` 索引、增加 `extension` 字段解决类型排序；后续再考虑 FTS5/n-gram、keyset 分页和 Windows USN Journal 增量索引。
- 本轮按用户要求仅给建议，未修改程序、未运行测试。
- 用户确认全量重建采用“按盘符并行扫描 + 有界队列 + 单 SQLite 写线程 + 大批量写入 + 导入后建索引”的方向。增量索引讨论倾向 Windows 混合方案：全量建立基线，运行期间监听文件变化，启动时通过 NTFS USN Journal 补齐离线变化；事件丢失、日志重置或非 NTFS 卷时执行局部校验或全量重建兜底。
- 已在 `file-searcher/file_searcher.py` 实现全量重建优化：最多 4 个盘符并行扫描、容量 20000 的有界队列、单 SQLite 写入端、5000 条批量插入、数据导入后创建二级索引、去除 AUTOINCREMENT，并增加全局构建互斥。整个重建使用单事务；取消或异常时回滚，保留旧索引。按用户偏好未运行程序或测试，仅执行 diff 静态检查。已本地提交 `ba00100`（`优化全量索引重建速度`），未推送。
- 按用户要求使用托管 Python 3.13.12 启动了 `file-searcher/file_searcher.py`，未做额外验证。
- 为解决 `ModuleNotFoundError: pystray`，创建隔离环境 `C:\Users\hjf\.workbuddy\binaries\python\envs\file-searcher`，安装 `pystray 0.19.5`、`Pillow 12.3.0`、`six 1.17.0`，并成功以后台任务方式运行 File Searcher。
- 按用户要求再次启动 File Searcher，后台任务 ID 为 `l3h7ws`。
- 用户实测并行全量重建“快了不少”，希望继续优化。下一轮建议优先考虑单盘内部的目录任务并发（保守默认 2～4 线程）、消除文件热路径中的重复 `stat/lower/splitext` 与日期字符串格式化，并加入分阶段耗时统计后再调整批次、队列和 SQLite 参数。
- 已增加全量索引耗时统计：使用 `time.perf_counter()` 记录扫描写入、数据库索引优化及总耗时，重建完成后在状态栏显示文件数和三项时间；取消仍回滚并保留旧索引。仅做静态 diff 检查，未运行程序或测试。已本地提交 `0ce3ee5`（`增加全量索引耗时统计`），未推送。
- 用户要求后续执行任务时主动展示关键进度节点，避免长时间无反馈。
- 修复耗时统计不显示：`_on_index_done()` 原先先设置耗时文本、随后 `_load_all()` 将其覆盖；现调整为先刷新列表、最后写入耗时状态。静态检查通过，未运行程序或测试。已本地提交 `acf581e`（`修复索引耗时状态栏显示`），未推送。
- 用户实测优化后全量重建耗时：扫描写入 2.7 秒、数据库索引优化 0.6 秒、总计 3.3 秒。当前全量重建已较快，继续做 Python/SQLite 微优化的绝对收益预计有限，应优先保持稳定；未来若需接近瞬时更新，应转向增量索引。
